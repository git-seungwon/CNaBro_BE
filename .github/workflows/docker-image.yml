name: Build, Push Docker Image, and Deploy with Docker Compose

on:
  push:
    branches:
      - main

jobs:
  build-push-deploy:
    runs-on: ubuntu-latest

    steps:
      # 1. 소스 체크아웃
      - name: Checkout code
        uses: actions/checkout@v3

      # 2. Docker Hub 로그인
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_ACCESS_TOKEN }}

      # 3. Docker 이미지 빌드 및 푸시
      - name: Build and Push Docker Image
        run: |
          docker build -t <docker_hub_username>/cnabro_be:latest .
          docker push <docker_hub_username>/cnabro_be:latest

      # 4. AWS EC2에 배포
      - name: Deploy to EC2
        run: |
          ssh -o StrictHostKeyChecking=no -i ${{ secrets.EC2_SSH_KEY }} ubuntu@<EC2_IP_ADDRESS> << 'EOF'
            cd /path/to/docker-compose/project
            docker-compose down
            docker-compose pull
            docker-compose up -d
          EOF
